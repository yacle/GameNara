<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
         "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="reply">
	<select id="list" resultType="org.game.nara.ReplyVO">
		select * from reply where bno=#{bno} order by rno desc
	</select>
	
	<insert id="create">
		insert into reply (rno, bno, replytext, replyer) values (reply_seq.nextval, #{bno},#{replytext},#{replayer})
	</insert>
	
	<update id="update">
		update reply set replytext=#{replytext}, updatedate = now() where rno=#{rno}
	</update>
	
	<delete id="delete">
		delete from reply where rno=#{rno}
	</delete>
	
</mapper>