<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
	<insert id="add" >
		insert into member values(#{id}, #{pwd}, #{email, jdbcType=VARCHAR}, 0)
	</insert>
	<insert id="add2" >
		 insert into member_info(id) values(#{id}) 
	</insert>
	
	<select id="check" resultType="int">
		select count(*) as cnt from member where id=#{id} and password=#{pwd}
	</select>
</mapper>