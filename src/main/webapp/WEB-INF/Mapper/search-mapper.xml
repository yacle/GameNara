<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="search">
 <select id="sellsearch" resultType="java.util.HashMap">
 select * from sell where title like #{search}
 </select>
 <select id="buysearch" resultType="java.util.HashMap">
 select * from buy where title like #{search}
 </select>
 <select id="freeboardsearch" resultType="java.util.HashMap">
 select * from freeboard where title like #{search}
 </select>
   <select id="aftersearch" resultType="java.util.HashMap">
 select * from after where title like #{search}
 </select>
 </mapper>